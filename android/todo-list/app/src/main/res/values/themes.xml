<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. -->
  <style name="Theme.TodoList" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Primary brand color. -->
    <item name="colorPrimary">@color/todo_grey_01</item>
    <item name="colorPrimaryVariant">@color/todo_grey_01</item>
    <item name="colorOnPrimary">@color/todo_black</item>
    <!-- Secondary brand color. -->

    <!-- Background color. -->
    <item name="background">@color/todo_grey_02</item>
    <item name="colorOnBackground">@color/todo_black</item>

    <!-- Status bar color. -->
    <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    <!-- Customize your theme here. -->
  </style>
  <style name="Theme.TodoList.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
  </style>
  <style name="Theme.TodoList.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
  <style name="Theme.TodoList.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
</resources>