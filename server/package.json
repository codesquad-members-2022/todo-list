{
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "json-sever": "^0.17.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch data.json --port 5000"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
